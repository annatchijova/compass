# Base URL of the COMPASS backend API. Read at build time by Next.js
# (NEXT_PUBLIC_* is inlined into the client bundle) and defaulted to
# http://localhost:8080 when unset.
NEXT_PUBLIC_API_URL=http://localhost:8080
